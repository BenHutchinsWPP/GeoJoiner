# Dependencies
node_modules/

# Build output
dist/

# Environment
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE / Editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# TypeScript
*.tsbuildinfo

# Test coverage
coverage/

# Vite / build cache
.vite/
.cache/

# us-zctas.geojson is ~155 MB, past GitHub's hard 100 MB per-file limit.
# Reproducible byte-for-byte with `node scripts/fetch-census.mjs`; the shipped
# .gjbf/.grid built from it ARE tracked, so only rebuilds need it.
data-src/us-zctas.geojson

# Raw geoBoundaries CGAZ + Natural Earth downloads, ~815 MB, plus the
# simplified intermediates. Re-fetched on demand by fetch-boundaries.mjs.
data-src/.cache/

# Project notes (not source)
memory/
_archive/
_Archive/

# AI tooling (not for release)
CLAUDE.md
HANDOFF.md
